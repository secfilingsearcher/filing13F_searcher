from setuptools import setup, find_packages

setup(
    name='filing13F_searcher',
    version='0.0.1',
    setup_requires=[
        'pytest-runner',
    ],
    tests_require=[
        'pytest',
    ]
)
